# Desktop simulation (Windows + macOS)
matplotlib>=3.8
simple-pid>=2.0      # optional reference; we use our own PID class
requests>=2.31       # ThingSpeak cloud upload

# Raspberry Pi (add when deploying to hardware)
# adafruit-ads1x15>=2.2
# RPi.GPIO>=0.7
# adafruit-blinka>=8.0
